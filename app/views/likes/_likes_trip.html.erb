<% @likes.each do |like| %>
  <div class = "col-lg-3">
    <div><%= like.user.name %> <span class="text-muted">posted at <%= like.created_at %></span></div>
    <div><%= link_to like.where, like %></div>
  </div>
<% end %>